apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: ekonstat
  labels:
    app: dashboard
    app.kubernetes.io/part-of: ekonstat
spec:
  type: ClusterIP
  selector:
    app: dashboard
  ports:
    - port: 80
      targetPort: 80
